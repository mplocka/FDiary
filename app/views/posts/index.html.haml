#posts

	- @posts.each do |post|
		.post
			%h2.title= link_to post.title, post
			%p.date
				Published at
				= time_ago_in_words(post.created_at)
				by
				= post.user.email

			%p.buttons
				%span
					%i.fa.fa-comments-o
					= post.comments.count
					Comments
				%span	
					%i.fa.fa-thumbs-o-up
					= post.get_likes.size
					Likes

// link_to "New Post", new_post_path